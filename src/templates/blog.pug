extends ./layout.pug
block content
	
	//- blogPostsList is the variable passed in
	//- we use #{...} if we want to render text in html
	//- the blogPost is a variable used in the later syntax
	//- also note, you can use both the keyword 'for' and 'each'
	//-  to iterate over th list
	for blogPost in blogPostsList
		section.post
			.container
				.row
					.col-lg-8.col-lg-offset-2.text-center
						h2.section-heading #{blogPost.title}
						hr.light
						//- when using variables within the body of html, we use #{}
						p.text-faded #{blogPost.description}
						//- when using variables within attribute of a tag, we omit the #{}
						a(href='/blog/' + blogPost.title).btn.btn-default.btn-xl Read more!