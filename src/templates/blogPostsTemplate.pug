doctype html
html(lang='en')
	head
		title #{theBlogPostTitle.title}
	body
		section.post
			.container.text-right
				a(href="").text-faded View All
				.row
					.col-lg-8.col-lg-offset-2.text-center
						h2.section-heading #{theBlogPostTitle.title}

						hr.light

						p.text-faded
							| #{theBlogPostTitle.description}
						.article
							| #{theBlogPostTitle.description}
